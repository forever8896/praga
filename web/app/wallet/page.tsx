// Screen 8 — Wallet / earnings — the leather-bound ledger.
// Real Privy embedded-wallet balance + on-chain Tipped events.
import { WalletView } from "@/lib/wallet-view";

export default function WalletPage() {
  return <WalletView />;
}
