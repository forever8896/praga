// Screen 7 — Edit your seal. Real form: loads the caller's NameStone
// record, edits bio/location/avatar/skills, posts back via /api/update-profile.
import { EditForm } from "@/lib/edit-form";

export default function EditPage() {
  return <EditForm />;
}
