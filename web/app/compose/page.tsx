// Screen 4 — Offer / Request composer. Real form: posts to /api/update-profile
// to write or replace the user's `offers` text record on their subname.
import { ComposeForm } from "@/lib/compose-form";

export default function ComposePage() {
  return <ComposeForm />;
}
